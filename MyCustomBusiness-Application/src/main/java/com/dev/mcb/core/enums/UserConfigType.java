package com.dev.mcb.core.enums;

/**
 * Enum type to store each users config values
 */
public enum  UserConfigType {
    SALT("USER_SALT"),
    PRIVATE_KEY("USER_PRIVATE_KEY");

    UserConfigType(String label) {
    }
}
